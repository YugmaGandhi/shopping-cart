import { EmptyState } from '@/components/EmptyState';

/** Placeholder — the real register form (RHF + Zod) lands in Stage 2.5. */
export function RegisterPage() {
  return <EmptyState title="Sign up" description="Register form arrives in Stage 2.5." />;
}
