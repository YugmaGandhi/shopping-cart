import { EmptyState } from '@/components/EmptyState';

/** Placeholder — the real login form (RHF + Zod) lands in Stage 2.5. */
export function LoginPage() {
  return <EmptyState title="Log in" description="Login form arrives in Stage 2.5." />;
}
