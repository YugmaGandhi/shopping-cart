import { EmptyState } from '@/components/EmptyState';

/** Placeholder — the real admin product management UI lands in Stage 2.8. */
export function AdminPage() {
  return <EmptyState title="Admin" description="Product management arrives in Stage 2.8." />;
}
