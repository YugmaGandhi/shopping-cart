import { EmptyState } from '@/components/EmptyState';

/** Placeholder — the real product listing (search/sort/pagination) lands in Stage 2.6. */
export function ProductsPage() {
  return <EmptyState title="Products" description="Product listing arrives in Stage 2.6." />;
}
